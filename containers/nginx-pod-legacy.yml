apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx
spec:
  selector:
    app: nginx
  template:
    metadata:
      name: nginx
      labels:
        app: nginx
    spec:
      containers:
      - image: nginx:1.7.9
        name: nginx
